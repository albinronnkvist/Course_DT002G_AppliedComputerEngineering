name: TContainerizationRepositoryName
description: Provisions Azure containerization resources
runtime:
  name: dotnet
  options:
    binary: bin/Release/net9.0/TOrgName.TContainerizationRepositoryName.Pulumi.dll
config:
  pulumi:autonaming:
    value:
      pattern: ${project}-${stack}-${name}-${hex(6)}
